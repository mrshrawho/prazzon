function chunk(arr, size) {`n  const chunks = [];`n  for (let i = 0; i < arr.length; i += size) {`n    chunks.push(arr.slice(i, i + size));`n  }`n  return chunks;`n}`n`nfunction flatten(arr) {`n  return arr.reduce((acc, val) => Array.isArray(val) ? acc.concat(flatten(val)) : acc.concat(val), []);`n}`n`nfunction unique(arr) {`n  return [...new Set(arr)];`n}`n`nfunction capitalize(str) {`n  return str.charAt(0).toUpperCase() + str.slice(1);`n}`n`nfunction truncate(str, len) {`n  return str.length > len ? str.slice(0, len) + "..." : str;`n}`n`nfunction slugify(str) {`n  return str.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");`n}`n`nfunction deepClone(obj) {`n  return JSON.parse(JSON.stringify(obj));`n}`n`nfunction merge(target, source) {`n  return Object.assign({}, target, source);`n}`n`nfunction pick(obj, keys) {`n  return keys.reduce((acc, key) => {`n    if (obj[key] !== undefined) acc[key] = obj[key];`n    return acc;`n  }, {});`n}`n`nmodule.exports = { chunk, flatten, unique, capitalize, truncate, slugify, deepClone, merge, pick };